# TODO

- Add forward decalarations FIXED
- Fix Not Equal that is read as < and then > FIXED
- Fix Var section just before main begin statement (must be always) FIXED
- Fix the grammar (Var section, write and read stmts, forward keyword) FIXED

- Create semantic analyzer
- Create codeGen logic
